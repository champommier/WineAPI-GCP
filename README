To run API, Create Firestore database in GCP.
Setup environement variables for GCP ProjectID, Service Account eMail, Service Account PrivateKey

export PROJECTID=...
export CLIENTEMAIL=...
export PRIVATEKEY=...

then run npm start (after npm update)
